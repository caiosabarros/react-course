export const DisplayEthSupply = ({handleSubmit}) => {

    return (
        <form onSubmit={handleSubmit}>
        <label>
          Get Total Supply of Ether:
        </label>
        <input type="submit" value="Submit1" />
        </form>
    );
    
}
